{% extends 'base.html.twig' %}

{% block body %}
<h1>Новости</h1>
{% for item in news %}
    <p><a href="{{ path('news-item', {itemId: item.id}) }}">{{item.title}}</a></p>
{% endfor %}
{% endblock %}
